{% extends "base.html.twig" %}

{% block title %}Gerer mes Trajets{% endblock %}

{% block body %}
    <section id="services" class="services section-bg">
    <div class="container" data-aos="fade-up">
        {% for message in app.flashes('success') %}
            <div class='alert alert-success'>{{message}}</div>
        {% endfor %} 


        <div class="section-title">
            <h2>Gérer mes trajets</h2>
        </div>

        {% for message in app.flashes('success') %}
            <div class='alert alert-success'>{{message}}</div>
        {% endfor %}
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Origine</th>
                    <th>Destination</th>
                    <th>Date</th>
                    <th>Heure</th>
                    <th>Places</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for trajet in trajets %}
                    <tr>
                        <td>{{trajet.origine}}</td>
                        <td>{{trajet.destination}}</td>
                        <td>{{trajet.date|format_datetime('short', 'none', locale='en')}}</td>
                        <td>{{trajet.date|format_datetime('none', 'short', locale='en')}}</td>
                        <td>{{trajet.places}}</td>
                        <td>
                            <a href="{{path('profil_trajet_edit', {'id': trajet.id})}}">
                                <i class="fa fa-pencil" aria-hidden="true"></i>
                            </a>
                            <a href="{{path('profil_trajet_delete', {'id': trajet.id})}}">
                                <i class="fas fa-trash"></i>
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <div class="text-right">
            <a href="{{path('profil_trajet_new')}}" class="btn btn-primary">Proposer un trajet</a>
        </div>
    </div>
    </section>   

{% endblock %}
